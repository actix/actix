[package]
name = "actix"
version = "0.8.3"
authors = ["Nikolay Kim <fafhrd91@gmail.com>"]
description = "Actor framework for Rust"
readme = "README.md"
keywords = ["actor", "futures", "actix", "async", "tokio"]
homepage = "https://actix.rs"
repository = "https://github.com/actix/actix.git"
documentation = "https://docs.rs/actix/"
categories = ["network-programming", "asynchronous"]
license = "MIT/Apache-2.0"
exclude = [".gitignore", ".travis.yml", ".cargo/config", "appveyor.yml"]
edition = "2018"

[badges]
travis-ci = { repository = "actix/actix", branch = "master" }
appveyor = { repository = "actix/actix" }
codecov = { repository = "actix/actix", branch = "master", service = "github" }

[lib]
name = "actix"
path = "src/lib.rs"

[workspace]
members = ["examples/chat"]

[features]
default = ["resolver", "http"]

# dns resolver
resolver = ["trust-dns-resolver"]

# Adds assertion to prevent processing too many messages on event loop
mailbox_assert = []

# actix-http support
http = ["actix-http"]

[dependencies]
actix-rt = "1.0" 
actix_derive = ""
bytes = ""
crossbeam-channel = ""
derive_more = ""
futures = ""
log = ""
pin-project = ""
lazy_static = ""
bitflags = ""
smallvec = ""
parking_lot = ""
tokio = { version = "0.2", default-features=false, features=["rt-core", "rt-util", "io-driver", "tcp", "uds", "udp", "time", "signal", "sync"] }
tokio-util   = { version = "", features = ["full"] }

# actix-http support
actix-http = { version = "1.0.0-alpha.6", optional = true }

# dns resolver
trust-dns-resolver = { version = "0.18.0-alpha", optional = true, default-features = false }

[dev-dependencies]
doc-comment = ""

[profile.release]
lto = true
opt-level = 3
codegen-units = 1
